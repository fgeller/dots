* only use code comments for tricky lines
* lower case sql keywords and statements
* you must not use `go build` to verify changes, use `go vet` instead
* make sure you wrap completed beads in a git commit and close the beads when the task is complete
* git commit messages should be concise and to the point, not overly verbose
