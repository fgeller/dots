[general]
accounts = m2l,gmail,movio
pythonfile = ~/.offlineimap.py
maxsyncaccounts = 1

[Account m2l]
localrepository = m2llocal
remoterepository = m2lremote
status_backend = sqlite
postsynchook = /bin/bash ~/dots/tag-mail.sh

[Repository m2llocal]
type = Maildir
localfolders = ~/Mail/m2l

[Repository m2lremote]
type = IMAP
remotehost = box.m2l.io
remoteuser = fg@m2l.io
remotepasseval = authinfo('fg@m2l.io')
ssl = yes
maxconnections = 3
realdelete = no
sslcacertfile = /usr/local/etc/openssl/cert.pem


[Account gmail]
localrepository = gmaillocal
remoterepository = gmailremote
status_backend = sqlite
postsynchook = /bin/bash ~/dots/tag-mail.sh

[Repository gmaillocal]
type = Maildir
localfolders = ~/Mail/gmail

[Repository gmailremote]
type = Gmail
remotehost = imap.gmail.com
remoteuser = fgeller
remotepasseval = authinfo('fgeller@gmail.com')
ssl = yes
maxconnections = 3
realdelete = no
folderfilter = lambda foldername: foldername in ['[Gmail]/All Mail', '[Gmail]/Spam', 'INBOX']
sslcacertfile = /usr/local/etc/openssl/cert.pem

[Account movio]
localrepository = moviolocal
remoterepository = movioremote
status_backend = sqlite
postsynchook = /bin/bash ~/dots/tag-mail.sh

[Repository moviolocal]
type = Maildir
localfolders = ~/Mail/movio

[Repository movioremote]
type = Gmail
remotehost = imap.gmail.com
remoteuser = felix@movio.co
remotepasseval = authinfo('felix@movio.co')
ssl = yes
maxconnections = 3
realdelete = no
folderfilter = lambda foldername: foldername in ['[Gmail]/All Mail', '[Gmail]/Spam', 'INBOX']
sslcacertfile = /usr/local/etc/openssl/cert.pem
